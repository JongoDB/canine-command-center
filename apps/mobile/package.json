{
  "name": "@ccc/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Canine Command Center — mobile client (Expo / React Native, iOS + Android). Shares domain logic with web via @ccc/shared and design tokens via @ccc/ui.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "echo 'The mobile app is implemented in milestone M0.6.' && exit 0",
    "build": "echo 'The mobile build (EAS) is implemented in milestone M0.6.'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@ccc/shared": "workspace:*",
    "@ccc/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
