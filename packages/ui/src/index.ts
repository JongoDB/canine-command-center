// Public surface of @ccc/ui — shared design tokens + a small seed of
// framework-agnostic style objects. Cross-platform component primitives (Badge,
// ScreenHeader, TimelinePhase, TrackGroup, CommandCard, TrainerNote,
// WarningNote — see docs/DESIGN.md §4) land alongside the real screens in Phase 1.
export * from './tokens';
export * from './styles';
